mtype = { takeThermalPicture, takeAmbientTemperature, takeCO2Reading, moveToRelativeLocation, moveToGPSLocation, orientRobotHeading, activateSeeder, deactivateSeeder, selectSeed }; // available action pool for robot

typedef Action {
    mtype actionType; // type of action
};

typedef Task {
    Action action;
};
