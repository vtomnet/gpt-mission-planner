mtype = { TakeThermalPicture, TakeAmbientTemperature, TakeCO2Reading, MoveToRelativeLocation, MoveToGPSLocation, OrientRobotHeading, DetectObject, ActivateSeeder, DeactivateSeeder, SelectSeed }; // available action pool for robot

typedef Action {
    mtype actionType; // type of action
};

typedef Task {
    Action action;
};
